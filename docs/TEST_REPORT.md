# NES 模拟器测试报告

测试时间：2025-11-30
测试 ROM：Donkey Kong (16KB PRG ROM, 8KB CHR ROM)
测试周期：2,000,000 主时钟周期

## ✅ 测试通过项

### 1. CPU 功能
- ✅ 复位向量正确加载：$C79E
- ✅ 指令执行正常（包括 3 字节指令）
- ✅ LDA $2002 正确读取 PPUSTATUS
- ✅ AND #$80 正确测试 VBlank 标志
- ✅ BEQ 条件跳转正常工作
- ✅ CPU 成功跳出 VBlank 等待循环

### 2. PPU 功能
- ✅ PPU 时钟正常运行（5.37 MHz）
- ✅ 扫描线计数正常（0-261）
- ✅ VBlank 信号正确生成（scanline 241）
- ✅ VBlank 在 5 帧中都正常触发
- ✅ PPUSTATUS ($2002) 正确返回 VBlank 标志

### 3. 跨时钟域同步
- ✅ 双触发器同步器工作正常
- ✅ vblank 信号从 ppu_clk 正确同步到 cpu_clk
- ✅ ppustatus[7] 正确设置和清除

### 4. 内存映射
- ✅ PRG ROM 正确映射（$8000-$FFFF）
- ✅ 16KB ROM 镜像正常工作
- ✅ PPU 寄存器正确映射（$2000-$2007）
- ✅ RAM 访问正常（$0000-$1FFF）

### 5. PPU 寄存器
- ✅ $2000 (PPUCTRL) 写入正常
- ✅ $2002 (PPUSTATUS) 读取正常
- ✅ 读取 $2002 后 VBlank 标志正确清除

## 📊 执行统计

```
总周期数：2,000,000
VBlank 触发次数：5 次
VRAM 写入次数：0 次（初始化阶段未完成）
NMI 触发次数：0 次（PPUCTRL NMI 未使能）
```

## 🔍 CPU 执行地址范围

```
0x0000 - 0x07FF   : RAM 访问
0x2002            : PPUSTATUS 读取
0x070E - 0x07AE   : RAM/栈操作
0xC7A8 - 0xC7BD   : ROM 代码执行
```

## 📝 观察到的行为

1. **初始化阶段**：
   - CPU 从复位向量 $C79E 开始执行
   - 设置 PPUCTRL = $10 (背景图案表 = 1, NMI 禁用)
   - 进入 VBlank 等待循环

2. **VBlank 检测**：
   - CPU 轮询 $2002 等待 VBlank
   - 成功检测到 VBlank 标志
   - 跳出循环继续执行

3. **后续执行**：
   - CPU 执行到 $C7BA-$C7BD 区域
   - 访问 RAM 地址 $070E-$07AE
   - 在多个 VBlank 周期间持续运行

## ⚠️ 已知限制

1. **NMI 未实现**：PPUCTRL bit 7 = 0，NMI 中断未使能
2. **VRAM 未初始化**：ROM 代码尚未写入 VRAM
3. **图形渲染未测试**：PPU 渲染逻辑未完全验证
4. **APU 未测试**：音频处理器未验证

## ✅ 核心问题修复

### 问题 1：CPU 3 字节指令时序错误
**症状**：LDA $2002 读取错误地址 $02AD
**原因**：EXECUTE 状态使用了错误的操作数字节
**修复**：添加 MEMORY 状态处理高字节读取

### 问题 2：ppustatus 清除时序
**症状**：CPU 可能读不到 VBlank 标志
**原因**：读取和清除在同一时钟周期
**修复**：延迟一个周期清除标志

## 🎯 结论

**NES 模拟器核心功能正常工作！**

- CPU 和 PPU 正确协同工作
- VBlank 同步机制正常
- 内存映射正确
- 基本指令执行正常

下一步可以：
1. 实现 NMI 中断处理
2. 完善 PPU 渲染逻辑
3. 添加更多 CPU 指令
4. 测试更复杂的 ROM
