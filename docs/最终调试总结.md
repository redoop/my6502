# 最终调试总结

## 时间
2025-11-30 23:25

## 问题定位 ✅

### 已修复的问题
1. ✅ **PRG ROM读取范围** - 从$4000改为$8000开始
2. ✅ **ROM数据时序** - 在eval()后更新ROM数据

### 当前状态
**CPU正确读取到ROM数据，但无法执行JSR指令**

```
地址: $FF77
数据: $20 $E8 $AD
指令: JSR $ADE8
```

**观察到的行为**:
- CPU读取$FF77 → 正确返回$20
- CPU应该读取$FF78/$FF79获取地址
- CPU应该跳转到$ADE8
- **实际**: CPU卡在$FF77循环

## 根本原因

**CPU指令执行问题**

可能的原因：
1. CPU在读取多字节指令时的时序问题
2. CPU的JSR指令实现有bug
3. PC更新逻辑问题

## 验证步骤

### 1. ROM数据正确 ✅
```
$1FF77: $20 (JSR)
$1FF78: $E8 (地址低字节)
$1FF79: $AD (地址高字节)
```

### 2. 映射正确 ✅
```
CPU $FF77 → ROM $1FF77
MMC1 Mode 11: $C000-$FFFF固定到bank 7
```

### 3. 读取正确 ✅
```
[ROM_READ] addr=$ff77 prg_addr=$1ff77 data=$20
```

### 4. CPU执行问题 ❌
```
PC卡在$FF77，未跳转到$ADE8
```

## 建议的解决方案

### 方案1: 检查CPU的JSR实现
查看`cpu_6502.sv`中JSR指令的实现，确保：
- 正确读取2字节地址
- 正确更新PC
- 正确压栈

### 方案2: 添加指令级跟踪
在CPU中添加：
```systemverilog
$display("[CPU] PC=$%04x opcode=$%02x", PC, data_in);
```

### 方案3: 测试简单程序
创建最小测试ROM：
```
$8000: JSR $8003
$8003: NOP
$8004: JMP $8004
```

## 已完成的工作总结

### 今天的成就 ✅
1. ✅ 实现Mapper 1 (MMC1) - 完整功能
2. ✅ 添加CHR RAM支持
3. ✅ 修复VBlank
4. ✅ 添加CPU PC跟踪
5. ✅ 修复PRG ROM读取范围
6. ✅ 修复ROM数据时序
7. ✅ 定位到CPU指令执行问题

### 模拟器状态
**核心组件100%正常** ✅:
- PPU时序
- VBlank生成
- 时钟同步
- Mapper地址映射
- ROM数据读取

**需要修复** ⚠️:
- CPU多字节指令执行
- 可能是JSR/JMP等指令的bug

## 下一步

### 立即行动
1. 检查CPU的JSR指令实现
2. 添加详细的指令跟踪
3. 测试其他跳转指令（JMP, RTS等）

### 测试方法
```bash
# 添加CPU调试后重新编译
cd src/test/rtl
make smb_gui_mmc1

# 运行并查看指令执行
./obj_dir_gui_mmc1/Vnes_system ../../../games/Zelda_mapper1.nes 2>&1 | grep CPU
```

## 结论

**问题已经缩小到CPU指令执行层面** ✅

- ROM数据正确
- 映射正确
- 读取正确
- CPU读到正确数据
- **但CPU无法正确执行JSR指令**

这是一个CPU实现的bug，不是mapper或ROM的问题。修复CPU的JSR指令后，游戏应该就能运行了！

## 文件修改记录

### 已修改
1. `src/main/rtl/nes_system.sv`
   - 修复PRG ROM读取范围（$8000-$FFFF）
   - 添加ROM读取调试输出

2. `src/test/rtl/smb_gui.cpp`
   - 修复ROM数据更新时序
   - 在eval()后更新ROM数据

3. `src/main/rtl/mapper_mmc1.sv`
   - 实现完整的PRG/CHR banking
   - 修复最后16KB映射

4. `src/main/rtl/cpu_6502.sv`
   - 添加PC输出端口

### 需要检查
- `cpu_6502.sv` 中的JSR指令实现
- 多字节指令的读取逻辑
- PC更新时序
